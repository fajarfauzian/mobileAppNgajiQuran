import 'package:flutter/material.dart';

class AmalanScreen extends StatelessWidget {
  const AmalanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Halaman Amalan"),
    );
  }
}
