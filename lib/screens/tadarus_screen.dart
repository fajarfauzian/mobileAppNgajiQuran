import 'package:flutter/material.dart';

class TadarusScreen extends StatelessWidget {
  const TadarusScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Halaman Tadarus"),
    );
  }
}
